local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Workspace = game:GetService("Workspace")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

local Window = Rayfield:CreateWindow({
   Name = "Farm v8 (Física Otimizada) 🚀🍓👽🟡",
   LoadingTitle = "Iniciando...",
   LoadingSubtitle = "Velocidade Recomendada: 39",
   ConfigurationSaving = { Enabled = false },
   KeySystem = false
})

local FarmTab = Window:CreateTab("🍓/👽 Fly Farm", 4483362458)
local FlyTab = Window:CreateTab("✈️ Voo Manual", 4483362458)

-- Variáveis de controle (Padrão atualizado para 39)
_G.AutoFarmGoldenBerry = false
_G.AutoFarmBerry = false
_G.AutoFarmAlien = false
_G.VelocidadeFarm = 39 -- Fixado em 39 para máxima eficiência estável
_G.BypassWalls = false
_G.AlturaHover = 7
_G.AlturaSeguraCeu = 110

_G.VooLivreAtivo = false
_G.VelocidadeVoo = 39

-- Noclip para o boneco não bater nas coisas enquanto voa fisicamente
local noclipConnection = nil
local function ativarNoclipLocal(status)
    if status then
        if not noclipConnection then
            noclipConnection = RunService.Stepped:Connect(function()
                local character = player.Character
                if character then
                    for _, part in pairs(character:GetChildren()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                end
            end)
        end
    else
        if noclipConnection then
            noclipConnection:Disconnect()
            noclipConnection = nil
        end
    end
end

-- ================= MOVIMENTAÇÃO POR VELOCIDADE FÍSICA CONSTANTE ================= --
local function voarAteAlvoFisico(alvo, velocidade)
    local character = player.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    local humanoid = character and character:FindFirstChildWhichIsA("Humanoid")
    if not rootPart or not humanoid or not alvo or not alvo.Parent then return end

    ativarNoclipLocal(true)

    -- Define a altura alvo baseada nas suas configurações
    local alturaAlvo = alvo.Position.Y + _G.AlturaHover - 5
    if _G.BypassWalls then
        alturaAlvo = _G.AlturaSeguraCeu
    end

    -- Loop de aproximação física
    while _G.AutoFarmGoldenBerry or _G.AutoFarmAlien or _G.AutoFarmBerry do
        if not alvo or not alvo.Parent or not rootPart then break end

        local destino = Vector3.new(alvo.Position.X, alturaAlvo, alvo.Position.Z)
        local direcao = (destino - rootPart.Position)
        local distancia = direcao.Magnitude

        -- Se chegou muito perto do ponto do alvo, sai do loop para pegar a próxima fruta
        if distancia < 1.5 then 
            break 
        end

        -- Olha com segurança para o lado do alvo (sem inclinar para baixo)
        local olharSeguro = Vector3.new(destino.X, rootPart.Position.Y, destino.Z)
        rootPart.CFrame = CFrame.lookAt(rootPart.Position, olharSeguro)

        -- Aplica velocidade física linear em direção ao alvo
        rootPart.AssemblyLinearVelocity = direcao.Unit * velocidade

        task.wait(0.02)
    end

    -- Para o boneco imediatamente ao terminar o trajeto
    if rootPart then
        rootPart.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
    end
    
    ativarNoclipLocal(false)
end

-- ================= FUNÇÃO DE BUSCA DE ALVOS ================= --
local function pegarAlvoMaisProximo(nomePasta, nomeItem, corObrigatoria)
    local pasta = Workspace:FindFirstChild(nomePasta)
    local character = player.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")

    if pasta and rootPart then
        local alvo = nil
        local menorDistancia = math.huge

        for _, item in pairs(pasta:GetChildren()) do
            if item.Name == nomeItem then
                local targetPart = item:IsA("BasePart") and item or item:FindFirstChildWhichIsA("BasePart")
                
                if targetPart then
                    local corValida = true
                    if corObrigatoria and targetPart.BrickColor.Name ~= "Deep orange" then
                        corValida = false
                    end

                    if corValida then
                        local dist = (rootPart.Position - targetPart.Position).Magnitude
                        if dist < menorDistancia then
                            menorDistancia = dist
                            alvo = targetPart
                        end
                    end
                end
            end
        end
        return alvo
    end
    return nil
end

-- ================= LOOP DO FLY FARM ================= --
task.spawn(function()
    while true do
        local alvoAtual = nil
        
        if _G.AutoFarmGoldenBerry then
            alvoAtual = pegarAlvoMaisProximo("Berries", "Berry", true)
        elseif _G.AutoFarmAlien then
            alvoAtual = pegarAlvoMaisProximo("Aliens", "Alien")
        elseif _G.AutoFarmBerry then
            alvoAtual = pegarAlvoMaisProximo("Berries", "Berry")
        end

        if alvoAtual then
            voarAteAlvoFisico(alvoAtual, _G.VelocidadeFarm)
        end
        
        task.wait(0.1)
    end
end)

-- ================= LOOP DO VOO MANUAL ================= --
local bodyVelocity = nil
local bodyGyro = nil

RunService.RenderStepped:Connect(function()
    local character = player.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    local humanoid = character and character:FindFirstChildWhichIsA("Humanoid")
    local camera = Workspace.CurrentCamera

    if _G.VooLivreAtivo and rootPart and humanoid and camera then
        if not bodyVelocity or bodyVelocity.Parent ~= rootPart then
            bodyVelocity = Instance.new("BodyVelocity")
            bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            bodyVelocity.Parent = rootPart
        end
        
        if not bodyGyro or bodyGyro.Parent ~= rootPart then
            bodyGyro = Instance.new("BodyGyro")
            bodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
            bodyGyro.D = 100
            bodyGyro.P = 10000
            bodyGyro.Parent = rootPart
        end

        bodyGyro.CFrame = camera.CFrame

        local direcao = humanoid.MoveDirection
        if direcao.Magnitude > 0 then
            bodyVelocity.Velocity = direcao * _G.VelocidadeVoo
        else
            bodyVelocity.Velocity = Vector3.new(0, 0, 0)
        end
    else
        if bodyVelocity then bodyVelocity:Destroy() bodyVelocity = nil end
        if bodyGyro then bodyGyro:Destroy() bodyGyro = nil end
    end
end)

-- ================= INTERFACE ================= --
FarmTab:CreateSection("🛸 CONFIGURAÇÃO DO FLY-FARM (FÍSICA ATIVA)")

FarmTab:CreateToggle({
   Name = "ATIVAR FARM DE BERRY (DINHEIRO) 🟡",
   CurrentValue = false,
   Callback = function(Value) _G.AutoFarmGoldenBerry = Value end,
})

FarmTab:CreateToggle({
   Name = "ATIVAR FARM DE ALIEN 👽",
   CurrentValue = false,
   Callback = function(Value) _G.AutoFarmAlien = Value end,
})

FarmTab:CreateToggle({
   Name = "ATIVAR FARM DE BERRY (COMUM) 🍓",
   CurrentValue = false,
   Callback = function(Value) _G.AutoFarmBerry = Value end,
})

FarmTab:CreateToggle({
   Name = "Modo Altura Máxima (Bypass de Paredes)",
   CurrentValue = false,
   Callback = function(Value) _G.BypassWalls = Value end,
})

-- Ajustado o valor inicial para 39
FarmTab:CreateSlider({
   Name = "Velocidade de Voo do Farm",
   Range = {10, 60},
   Increment = 1,
   Suffix = "Vel",
   CurrentValue = 39,
   Callback = function(Value) _G.VelocidadeFarm = Value end,
})

FarmTab:CreateSlider({
   Name = "Altura do Voo Rasante (Hover)",
   Range = {2, 20},
   Increment = 1,
   Suffix = "Studs",
   CurrentValue = 7,
   Callback = function(Value) _G.AlturaHover = Value end,
})

FarmTab:CreateSlider({
   Name = "Altura de Rota Segura (Por Cima)",
   Range = {80, 180},
   Increment = 5,
   Suffix = "Studs",
   CurrentValue = 110,
   Callback = function(Value) _G.AlturaSeguraCeu = Value end,
})

FlyTab:CreateSection("🛸 CONTROLE DE VOO LIVRE")

FlyTab:CreateToggle({
   Name = "Ativar Voo Manual",
   CurrentValue = false,
   Callback = function(Value) _G.VooLivreAtivo = Value end,
})

FlyTab:CreateSlider({
   Name = "Velocidade",
   Range = {15, 50},
   Increment = 1,
   Suffix = "Vel",
   CurrentValue = 39,
   Callback = function(Value) _G.VelocidadeVoo = Value end,
})

Rayfield:LoadConfiguration()
