Local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Workspace = game:GetService("Workspace")
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

local Window = Rayfield:CreateWindow({
   Name = "Farm v8 🚀🍓👽🟡",
   LoadingTitle = "Restaurando Estabilidade...",
   LoadingSubtitle = "Studs Manuais | Anti-Queda | Anti-Fantasma",
   ConfigurationSaving = { Enabled = false },
   KeySystem = false
})

local FarmTab = Window:CreateTab("🍓/👽 Fly Farm", 4483362458)
local FlyTab = Window:CreateTab("✈️ Voo Manual", 4483362458)

-- Variáveis de controle
_G.AutoFarmGoldenBerry = false
_G.AutoFarmBerry = false
_G.AutoFarmAlien = false
_G.VelocidadeFarm = 31
_G.BypassWalls = false
_G.AlturaHover = 7 -- VOLTOU!
_G.AlturaSeguraCeu = 110

_G.VooLivreAtivo = false
_G.VelocidadeVoo = 31

-- Garante que o boneco não se machuque ou prenda em texturas locais
local noclipConnection = nil
local function ativarNoclipLocal(status)
    if status then
        if not noclipConnection then
            noclipConnection = RunService.Stepped:Connect(function()
                local character = player.Character
                if character then
                    for _, part in pairs(character:GetChildren()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                end
            end)
        end
    else
        if noclipConnection then
            noclipConnection:Disconnect()
            noclipConnection = nil
        end
    end
end

-- ================= SISTEMA DE VOO DOS DEUSES (TWEEN FLYING) ================= --
local function voarAteAlvo(alvo, velocidade)
    local character = player.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    if not rootPart or not alvo or not alvo.Parent then return end

    ativarNoclipLocal(true)

    if _G.BypassWalls then
        -- Rota Alta (Céu)
        local posSubida = CFrame.new(rootPart.Position.X, _G.AlturaSeguraCeu, rootPart.Position.Z)
        local t1 = TweenService:Create(rootPart, TweenInfo.new((rootPart.Position - posSubida.Position).Magnitude / velocidade, Enum.EasingStyle.Linear), {CFrame = posSubida})
        t1:Play() t1.Completed:Wait()

        local posCeuAlvo = CFrame.new(alvo.Position.X, _G.AlturaSeguraCeu, alvo.Position.Z)
        local t2 = TweenService:Create(rootPart, TweenInfo.new((rootPart.Position - posCeuAlvo.Position).Magnitude / velocidade, Enum.EasingStyle.Linear), {CFrame = posCeuAlvo})
        t2:Play() t2.Completed:Wait()

        local t3 = TweenService:Create(rootPart, TweenInfo.new((rootPart.Position - alvo.Position).Magnitude / velocidade, Enum.EasingStyle.Linear), {CFrame = alvo.CFrame})
        t3:Play()
        
        while t3.PlaybackState == Enum.PlaybackState.Playing do
            if not alvo or not alvo.Parent then t3:Cancel() break end
            task.wait(0.05)
        end
    else
        -- VOO RASANTE SEGURO (COM CONTROLE DE STUDS DE VOLTA)
        local posicaoVooRasante = Vector3.new(alvo.Position.X, alvo.Position.Y + _G.AlturaHover - 5, alvo.Position.Z)
        local dist = (rootPart.Position - posicaoVooRasante).Magnitude
        
        if dist > 0.5 then
            -- Vira o corpo APENAS NA HORIZONTAL (Garante que não vai apontar pro chão e cair do mapa)
            local olharSeguro = Vector3.new(posicaoVooRasante.X, rootPart.Position.Y, posicaoVooRasante.Z)
            rootPart.CFrame = CFrame.lookAt(rootPart.Position, olharSeguro)
            
            -- Mantém a rotação e viaja até o alvo na altura certa
            local cfDestino = CFrame.new(posicaoVooRasante) * rootPart.CFrame.Rotation
            local t = TweenService:Create(rootPart, TweenInfo.new(dist / velocidade, Enum.EasingStyle.Linear), {CFrame = cfDestino})
            t:Play()
            
            -- ANTI-FANTASMA
            while t.PlaybackState == Enum.PlaybackState.Playing do
                if not alvo or not alvo.Parent then
                    t:Cancel()
                    break
                end
                task.wait(0.02)
            end
        end
    end

    ativarNoclipLocal(false)
end

-- ================= FUNÇÃO DE BUSCA DE ALVOS ================= --
local function pegarAlvoMaisProximo(nomePasta, nomeItem, corObrigatoria)
    local pasta = Workspace:FindFirstChild(nomePasta)
    local character = player.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")

    if pasta and rootPart then
        local alvo = nil
        local menorDistancia = math.huge

        for _, item in pairs(pasta:GetChildren()) do
            if item.Name == nomeItem then
                local targetPart = item:IsA("BasePart") and item or item:FindFirstChildWhichIsA("BasePart")
                
                if targetPart then
                    local corValida = true
                    
                    if corObrigatoria then
                        if targetPart.BrickColor.Name ~= "Deep orange" then
                            corValida = false
                        end
                    end

                    if corValida then
                        local dist = (rootPart.Position - targetPart.Position).Magnitude
                        if dist < menorDistancia then
                            menorDistancia = dist
                            alvo = targetPart
                        end
                    end
                end
            end
        end
        return alvo
    end
    return nil
end

-- ================= LOOP DO FLY FARM ================= --
task.spawn(function()
    while true do
        local alvoAtual = nil
        
        if _G.AutoFarmGoldenBerry then
            alvoAtual = pegarAlvoMaisProximo("Berries", "Berry", true)
        elseif _G.AutoFarmAlien then
            alvoAtual = pegarAlvoMaisProximo("Aliens", "Alien")
        elseif _G.AutoFarmBerry then
            alvoAtual = pegarAlvoMaisProximo("Berries", "Berry")
        end

        if alvoAtual then
            voarAteAlvo(alvoAtual, _G.VelocidadeFarm)
            task.wait(0.01)
        end
        
        task.wait(0.1)
    end
end)

-- ================= LOOP DO VOO MANUAL ================= --
local bodyVelocity = nil
local bodyGyro = nil

RunService.RenderStepped:Connect(function()
    local character = player.Character
    local rootPart = character and character:FindFirstChild("HumanoidRootPart")
    local humanoid = character and character:FindFirstChildWhichIsA("Humanoid")
    local camera = Workspace.CurrentCamera

    if _G.VooLivreAtivo and rootPart and humanoid and camera then
        if not bodyVelocity or bodyVelocity.Parent ~= rootPart then
            bodyVelocity = Instance.new("BodyVelocity")
            bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            bodyVelocity.Parent = rootPart
        end
        
        if not bodyGyro or bodyGyro.Parent ~= rootPart then
            bodyGyro = Instance.new("BodyGyro")
            bodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
            bodyGyro.D = 100
            bodyGyro.P = 10000
            bodyGyro.Parent = rootPart
        end

        bodyGyro.CFrame = camera.CFrame

        local direcao = humanoid.MoveDirection
        if direcao.Magnitude > 0 then
            bodyVelocity.Velocity = direcao * _G.VelocidadeVoo
        else
            bodyVelocity.Velocity = Vector3.new(0, 0, 0)
        end
    else
        if bodyVelocity then bodyVelocity:Destroy() bodyVelocity = nil end
        if bodyGyro then bodyGyro:Destroy() bodyGyro = nil end
    end
end)

-- ================= INTERFACE ================= --

FarmTab:CreateSection("🛸 CONFIGURAÇÃO DO FLY-FARM")

FarmTab:CreateToggle({
   Name = "ATIVAR FARM DE BERRY (DINHEIRO) 🟡",
   CurrentValue = false,
   Callback = function(Value)
       _G.AutoFarmGoldenBerry = Value
   end,
})

FarmTab:CreateToggle({
   Name = "ATIVAR FARM DE ALIEN 👽",
   CurrentValue = false,
   Callback = function(Value)
       _G.AutoFarmAlien = Value
   end,
})

FarmTab:CreateToggle({
   Name = "ATIVAR FARM DE BERRY (COMUM) 🍓",
   CurrentValue = false,
   Callback = function(Value)
       _G.AutoFarmBerry = Value
   end,
})

FarmTab:CreateToggle({
   Name = "Modo Altura Máxima (Bypass de Paredes)",
   CurrentValue = false,
   Callback = function(Value)
       _G.BypassWalls = Value
   end,
})

FarmTab:CreateSlider({
   Name = "Velocidade de Voo do Farm",
   Range = {10, 45},
   Increment = 1,
   Suffix = "Vel",
   CurrentValue = 31,
   Callback = function(Value)
       _G.VelocidadeFarm = Value
   end,
})

-- O SLIDER VOLTOU PARA VOCÊ AJUSTAR!
FarmTab:CreateSlider({
   Name = "Altura do Voo Rasante (Hover)",
   Range = {2, 20},
   Increment = 1,
   Suffix = "Studs",
   CurrentValue = 7,
   Callback = function(Value)
       _G.AlturaHover = Value
   end,
})

FarmTab:CreateSlider({
   Name = "Altura de Rota Segura (Por Cima)",
   Range = {80, 180},
   Increment = 5,
   Suffix = "Studs",
   CurrentValue = 110,
   Callback = function(Value)
       _G.AlturaSeguraCeu = Value
   end,
})

FlyTab:CreateSection("🛸 CONTROLE DE VOO LIVRE")

FlyTab:CreateToggle({
   Name = "Ativar Voo Manual",
   CurrentValue = false,
   Callback = function(Value)
       _G.VooLivreAtivo = Value
   end,
})

FlyTab:CreateSlider({
   Name = "Velocidade",
   Range = {15, 50},
   Increment = 1,
   Suffix = "Vel",
   CurrentValue = 31,
   Callback = function(Value)
       _G.VelocidadeVoo = Value
   end,
})
